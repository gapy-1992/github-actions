name: check out project

on: [push]
jobs:
  list-file:
    runs-on: ubuntu-latest
    steps:
      - name: list files
        id: list-file
        run: |
          pwd
          ls 
          echo $GITHUB_SHA
          echo $GITHUB_WORKSPACE
      - name: checkout-step
        uses: actions/checkout@v1
